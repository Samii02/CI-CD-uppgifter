name: Hello CI

on:
  push:
    branches: ["main"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: setup node
        uses: actions/setup-node@v3 #Sätter upp e node enviroment med v20
        with:
          node-version: "20"

      - name: Install dependecies
        run: npm install # Installerar alla beroenden i package.json

      - name: Print out line
        run: echo "Hello from CI pipeline" # test
